apiVersion: v1
kind: Service
metadata:
  name: {{ include "panelarr.fullname" . }}
  labels:
    {{- include "panelarr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "panelarr.selectorLabels" . | nindent 4 }}
